#pragma once

// Compare two arrays of integers of the same length
void compareArrays(int* a, int* b, int length);

// Compare three arrays of integers of the same length
void compareArrays(int* a, int* b, int* c, int length);

// Print the contents of an array of integers
void printArray(int* a, int length);